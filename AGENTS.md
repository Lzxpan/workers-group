# AGENTS.md

本 repository 發布 `orchestrating-workers-group` 的公開套件。

- `SKILL.md`、`agents/openai.yaml` 與 `.codex/agents/` 必須維持與權威來源一致。
- 公開樹只保留 README 所列的安裝 manifest；新增檔案前先確認它是執行或使用所必需。
- 文件使用繁體中文；技術名稱、路徑、檔名與設定鍵保留原文。
- 變更後至少執行格式或 parser check，並確認 UTF-8、LF、無 BOM 與無 U+FFFD。
