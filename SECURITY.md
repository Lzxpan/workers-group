# 安全說明

打工人集團會安裝掛勾（Hooks）。掛勾是在 Codex 做事前後自動執行的小程式。它能幫你擋下危險操作，但也代表它不是普通的說明文件。

## 安裝前請做三件事

1. 先執行 `pwsh -File .\scripts\install-global.ps1 -WhatIf`。這是安全預演（WhatIf），只告訴你將做什麼，不修改檔案。
2. 看清楚它要複製的檔案和備份位置。
3. 如果你看不懂某一行指令，先不要執行正式安裝。

## 這個專案刻意不做的事

- 不上傳你的程式碼、資料庫或祕密資料。
- 不覆寫整份 `config.toml` 設定檔。
- 不讀取 `.env`、私鑰（private key）或其他祕密檔案。
- 不在遇到同名但內容不同的掛勾時自動覆寫。

## 回報問題

請不要在 GitHub Issue 貼上密碼、API 金鑰、存取權杖（access token）、`.env` 檔案或私鑰。若發現可能造成資料外洩或可繞過安全檢查的問題，請使用 GitHub 的私密安全通報功能，或先聯絡 repository owner。
